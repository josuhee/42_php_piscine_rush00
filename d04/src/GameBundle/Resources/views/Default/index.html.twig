<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <style>
            body{
                background-image:url(background.png);
            }
            table{
                border: 2px solid; border-collapse: collapse;
            }
            th, td{
                border: 1px solid;
                color: white;
                font-size: 2em;
            }
        </style>
    </head>
    <body>
        <table>  
                <tr>
                    <td width = "1000px" height = "800px"><img src = {{ app.session.get('moviemon')[4]['post'] }} width = "900px" height = "800px"></td>
                    <td width = "500px" height = "700px">
                    {{ app.session.get('moviemon')[4]['health'] }}
                    {{ form_start(form) }}
                    {{ form_end(form) }}
                    </td>
                </tr>
                <tr>
                    <td width = "1000px" height = "300px" >{{ app.session.get('moviemon')[4]['title'] }}</td>
                    <td></td>
                </tr>
        </table>  
    </body>
</html>
